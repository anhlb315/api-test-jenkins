const catchAsync = require("../utils/catchAsync");
const AppError = require("../utils/appError");
const { Job, JobImage } = require("../models/databaseIndex");

exports.importImagesToAllJobs = catchAsync(async (req, res, next) => {
  return;
});
